decscribe("intern")

it("should set the job title to 'intern'", () => {
    expect();
});

it("should prompt the user for intern's full name", () => {
    expect();
});

it("should prompt the user for intern's ID mumber", () => {
    expect();
});

it("should prompt user for intern's email", () => {
    expect();
});

it("should prompt the user for the intern's school name", () => {
    expect();
});

it("should prompt the user to select engineer or intern or to finish building the team profile", () => {
    expect();
});